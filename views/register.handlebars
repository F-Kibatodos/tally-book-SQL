<div class="container">
  <div class="row mt-5">
    <div class="col-md-6 m-auto">
      <div class="card card-body">
        <h2 class="text-center mb-3">
          註冊
        </h2>
        <form action="/user/register" method="POST">
          <div class="form-group">
            <label for="name">使用者名稱</label>
            <input type="name" id="name" name="name" class="form-control" placeholder="頭尾不可以是空格，字元間不超過一個空格"
              value="{{{name}}}" />
          </div>
          <div class="form-group">
            <label for="email">信箱</label>
            <input type="email" id="email" name="email" class="form-control" placeholder="輸入信箱" value="{{{email}}}" />
          </div>
          <div class="form-group">
            <label for="password">密碼</label>
            <input type="password" id="password" name="password" class="form-control"
              placeholder="英文大小寫及數字至少各一，無特殊字符如：@#$%" value="{{{password}}}" />
          </div>
          <div class="form-group">
            <label for="password2">確認密碼</label>
            <input type="password" id="password2" name="password2" class="form-control" placeholder="確認密碼"
              value="{{{password2}}}" />
          </div>
          <button type="submit" class="btn btn-primary btn-block">
            註冊
          </button>
        </form>
        <p class="mt-4">已有帳戶？ <a href="/user/login">點我登入</a></p>
      </div>
    </div>
  </div>
</div>